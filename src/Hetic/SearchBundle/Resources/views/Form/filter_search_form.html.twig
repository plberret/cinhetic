<form action="{{ path('hetic_search_result') }}" method="get" accept-charset="utf-8">
	<div class="row">
		<div class="span3">
			<span>Trier par : </span>
			<select name="sort" id="sort">
				{{ knp_pagination_sortable(pagination, 'Id', 'a.id') }}
				{{ knp_pagination_sortable(pagination, 'Title', 'a.title') }}
			</select>
		</div>
		<div class="span3">
			<span>Résultats par page : </span>
			<select name="nb" id="nb">
				<option value="5" {% if limitPerPage == 5 %}selected{% endif %} >5</option>
				<option value="10" {% if limitPerPage == 10 %}selected{% endif %} >10</option>
				<option value="15" {% if limitPerPage == 15 %}selected{% endif %} >15</option>
				<option value="20" {% if limitPerPage == 20 %}selected{% endif %} >20</option>
				<option value="50" {% if limitPerPage == 50 %}selected{% endif %} >50</option>
				<option value="100" {% if limitPerPage == 100 %}selected{% endif %} >100</option>
			</select>
		</div>
		<input type="hidden" name="t" value="{{ searchQuery }}" />
	</div>
	<button class="btn btn-primary">Filtrer</button>
</form>